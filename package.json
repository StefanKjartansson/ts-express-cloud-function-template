{
  "name": "ts-express-cloud-function-template",
  "version": "0.0.1",
  "description": "Template for a express powered GCP cloud function in TypeScript",
  "repository": "https://github.com/StefanKjartansson/ts-express-cloud-function-template",
  "author": "Stefan Kjartansson",
  "license": "MIT",
  "private": false,
  "main": "dist/index.js",
  "scripts": {
    "start": "yarn run serve",
    "build": "yarn run build-ts",
    "serve": "node dist/server.js",
    "test": "jest --forceExit --coverage --verbose",
    "build-ts": "tsc",
    "deploy": "yarn run build && gcloud beta functions deploy project --entry-point handler --trigger-http --memory=256MB"
  },
  "dependencies": {
    "express": "^4.16.2"
  },
  "devDependencies": {
    "@types/express": "^4.11.1",
    "jest": "^22.4.0",
    "ts-jest": "^22.0.4",
    "ts-node": "^5.0.0",
    "typescript": "^2.7.2"
  }
}
